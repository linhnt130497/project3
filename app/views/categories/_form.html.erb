<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :style %>
    <%= form.text_field :style, id: :category_style %>
  </div>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user, id: :category_user %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :category_references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
